name: Deploy Production (Build on GitHub & Deploy Pre-built Images)

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: production_deploy
  cancel-in-progress: false

jobs:
  # -------------------------------------------------------------
  # 1. BUILD & VERIFY DOCKER IMAGES ON GITHUB ACTIONS
  # -------------------------------------------------------------
  build-and-push:
    name: Build & Verify Production Images
    runs-on: ubuntu-latest
    timeout-minutes: 20
    outputs:
      registry_prefix: ${{ steps.prep.outputs.registry_prefix }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Prepare Registry Variables
        id: prep
        run: |
          OWNER_LC=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          echo "OWNER_LC=${OWNER_LC}" >> $GITHUB_ENV
          echo "registry_prefix=ghcr.io/${OWNER_LC}/" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry (GHCR)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # --- 1a. Build Backend ---
      - name: Build & Push Backend
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/backend/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ env.OWNER_LC }}/areena-backend:latest
            ghcr.io/${{ env.OWNER_LC }}/areena-backend:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # --- 1b. Build WebSocket ---
      - name: Build & Push WebSocket
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/websocket-server/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ env.OWNER_LC }}/areena-websocket:latest
            ghcr.io/${{ env.OWNER_LC }}/areena-websocket:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # --- 1c. Build Frontend (Next.js) ---
      - name: Build & Push Frontend
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/frontend/Dockerfile
          build-args: |
            NEXT_PUBLIC_API_URL=/api
          push: true
          tags: |
            ghcr.io/${{ env.OWNER_LC }}/areena-frontend:latest
            ghcr.io/${{ env.OWNER_LC }}/areena-frontend:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # -------------------------------------------------------------
  # 2. ZERO-DOWNTIME DEPLOYMENT ON LIVE / DEMO VPS
  # -------------------------------------------------------------
  deploy-live:
    name: Deploy to Live Server (${{ matrix.server.name }})
    needs: build-and-push
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        server:
          # -------------------------------------------------------------
          # 1. Current Live / Demo Instance
          # -------------------------------------------------------------
          - name: "Demo Server"
            project_name: "areena-demo"
            host_key: "PROD_SSH_HOST"
            user_key: "PROD_SSH_USER"
            key_secret: "PROD_SSH_KEY"
            port_key: "PROD_SSH_PORT"      # Optional (defaults to 22)
            path_key: "PROD_DEPLOY_PATH"   # Optional (defaults to ~/areena)
            env_secret: "PROD_ENV"

          # -------------------------------------------------------------
          # 2. How to Add Future Productive Live Instances:
          # Simply uncomment and fill in the corresponding GitHub Variable/Secret names:
          # -------------------------------------------------------------
          # - name: "Live Production Server 1"
          #   project_name: "areena-prod-1"
          #   host_key: "PROD1_SSH_HOST"
          #   user_key: "PROD1_SSH_USER"
          #   key_secret: "PROD1_SSH_KEY"
          #   port_key: "PROD1_SSH_PORT"     # Optional
          #   path_key: "PROD1_DEPLOY_PATH"  # Optional (defaults to ~/areena)
          #   env_secret: "PROD1_ENV"

          # - name: "Live Production Server 2"
          #   project_name: "areena-prod-2"
          #   host_key: "PROD2_SSH_HOST"
          #   user_key: "PROD2_SSH_USER"
          #   key_secret: "PROD2_SSH_KEY"
          #   port_key: "PROD2_SSH_PORT"     # Optional
          #   path_key: "PROD2_DEPLOY_PATH"  # Optional (defaults to ~/areena)
          #   env_secret: "PROD2_ENV"

    steps:
      - name: Trigger Fast Image Pull & Restart via SSH
        uses: appleboy/ssh-action@v1.0.3
        env:
          APP_ENV_CONTENT: ${{ secrets[matrix.server.env_secret] || secrets.PROD_ENV }}
          USE_PREBUILT_IMAGES: "true"
          GHCR_USER: ${{ github.actor }}
          GHCR_TOKEN: ${{ secrets.GHCR_PULL_TOKEN || secrets.GITHUB_TOKEN }}
          REGISTRY_IMAGE_PREFIX: ${{ needs.build-and-push.outputs.registry_prefix }}
        with:
          host: ${{ vars[matrix.server.host_key] || secrets[matrix.server.host_key] }}
          username: ${{ vars[matrix.server.user_key] || secrets[matrix.server.user_key] }}
          key: ${{ secrets[matrix.server.key_secret] }}
          port: ${{ vars[matrix.server.port_key] || secrets[matrix.server.port_key] || 22 }}
          envs: APP_ENV_CONTENT,USE_PREBUILT_IMAGES,GHCR_USER,GHCR_TOKEN,REGISTRY_IMAGE_PREFIX
          script_stop: true
          script: |
            # Default deploy path to ~/areena if not specified
            TARGET_PATH="${{ (matrix.server.path_key && (vars[matrix.server.path_key] || secrets[matrix.server.path_key])) || '~/areena' }}"
            echo "🚀 Connecting to ${{ matrix.server.name }} at $TARGET_PATH..."
            cd "$TARGET_PATH"
            
            # Export registry image prefix to .env if not present
            if ! grep -q "REGISTRY_IMAGE_PREFIX" .env 2>/dev/null; then
              echo "REGISTRY_IMAGE_PREFIX=$REGISTRY_IMAGE_PREFIX" >> .env
            fi
            
            chmod +x scripts/deploy.sh
            ./scripts/deploy.sh main "${{ matrix.server.project_name }}"
