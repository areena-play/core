{
    # Global options
    email {$LETSENCRYPT_EMAIL}
    auto_https auto_mode
}

{$DOMAIN_NAME:localhost} {
    # 1. WebSocket endpoint (/ws and /ws/*)
    handle /ws* {
        reverse_proxy websocket:5000
    }

    # 2. Next.js Frontend & API Gateway
    handle {
        reverse_proxy frontend:3000
    }
}

